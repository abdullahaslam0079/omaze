/// Game phase states.
enum HopPhase { menu, play, dead }

/// Movement types available to the player.
enum HopMove { step, hop, leap }

/// The terrain kind for each world row.
enum RowKind { grass, road, water, rail }
